export function employeePayRates(monthly:number|null|undefined,divisor:number,hoursPerDay:number){const salary=Number(monthly??0);const daily=divisor>0?salary/divisor:0;const hourly=hoursPerDay>0?daily/hoursPerDay:0;return{daily,hourly,eightHours:hourly*8}}
export function workedMinutePay(minutes:number,hoursPerDay:number,hourly:number,extraHourly:number){const normalLimit=Math.round(hoursPerDay*60);const normalMinutes=Math.min(Math.max(0,minutes),normalLimit);const extraMinutes=Math.max(0,minutes-normalLimit);return{normalMinutes,extraMinutes,normalHours:normalMinutes/60,extraHours:extraMinutes/60,normalPay:normalMinutes/60*hourly,extraPay:extraMinutes/60*extraHourly}}
