import { useState } from 'react';
import { Eye, EyeOff, LockKeyhole } from 'lucide-react';
import { Navigate, useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
export function LoginPage(){const {session,login}=useAuth();const nav=useNavigate();const [user,setUser]=useState('admin'),[password,setPassword]=useState('Admin123!'),[show,setShow]=useState(false),[error,setError]=useState(''),[loading,setLoading]=useState(false);if(session)return <Navigate to="/dashboard" replace/>;async function submit(e:React.FormEvent){e.preventDefault();setError('');if(!user.trim()||!password){setError('Completa usuario y contraseña');return}setLoading(true);try{await login(user,password);nav('/dashboard')}catch(e){setError(e instanceof Error?e.message:'No fue posible iniciar sesión')}finally{setLoading(false)}}return <div className="login-page"><div className="login-art"><div className="orb"/><div className="login-copy"><span className="eyebrow">CONTROL HORARIO IA</span><h1>El tiempo de tu equipo,<br/><em>bajo control.</em></h1><p>Administra asistencia, empleados, reportes y nómina desde una experiencia empresarial segura.</p><div className="trust">● Sistema operativo <span>Datos demo · conexión API futura</span></div></div></div><form className="login-card" onSubmit={submit}><div className="login-logo"><span className="brand-mark">O</span><div><b>OSINET</b><small>TIME ERP ENTERPRISE</small></div></div><h2>Bienvenido</h2><p>Ingresa a tu espacio administrativo</p><label>Usuario<input value={user} onChange={e=>setUser(e.target.value)} autoComplete="username"/></label><label>Contraseña<div className="password"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/><button type="button" onClick={()=>setShow(!show)} aria-label="Mostrar contraseña">{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<div className="error">{error}</div>}<button className="primary full" disabled={loading}><LockKeyhole size={18}/>{loading?'Validando…':'Iniciar sesión'}</button><div className="demo-note"><b>Acceso de demostración</b><span>admin / Admin123!</span></div></form></div>}
