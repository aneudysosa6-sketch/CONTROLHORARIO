import{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';
export function PasswordRecoveryPage(){const{requestPasswordReset}=useAuth();const[email,setEmail]=useState(''),[message,setMessage]=useState(''),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setError('');try{await requestPasswordReset(email.trim().toLowerCase());setMessage('Si el correo existe, recibirás un enlace de recuperación.')}catch(e){setError(e instanceof Error?e.message:'No fue posible enviar el enlace.')}}return <div className="auth-page"><form className="auth-card" onSubmit={submit}><h1>Recuperar contraseña</h1><p>Te enviaremos un enlace seguro mediante Supabase Auth.</p><label>Correo<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label>{message&&<div className="success">{message}</div>}{error&&<div className="error">{error}</div>}<button className="primary full">Enviar enlace</button><Link className="auth-link" to="/login">Volver al inicio de sesión</Link></form></div>}
export function PasswordUpdatePage(){const{updatePassword}=useAuth();const nav=useNavigate();const[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();if(password.length<10){setError('La contraseña debe tener al menos 10 caracteres.');return}if(password!==confirm){setError('Las contraseñas no coinciden.');return}try{await updatePassword(password);nav('/dashboard')}catch(e){setError(e instanceof Error?e.message:'No fue posible cambiar la contraseña.')}}return <div className="auth-page"><form className="auth-card" onSubmit={submit}><h1>Nueva contraseña</h1><p>Usa una contraseña única de al menos 10 caracteres.</p><label>Nueva contraseña<input type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Confirmar<input type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></label>{error&&<div className="error">{error}</div>}<button className="primary full">Actualizar contraseña</button></form></div>}
export function AccessDeniedPage(){return <div className="denied"><span>403</span><h1>Acceso denegado</h1><p>Tu perfil no tiene el permiso necesario para abrir este módulo.</p><Link className="primary" to="/dashboard">Volver al inicio</Link></div>}
