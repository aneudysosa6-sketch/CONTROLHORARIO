import{createContext,useCallback,useContext,useEffect,useState,type ReactNode}from'react';import{authService,hydrateSession}from'../modules/auth/authService';import type{Session}from'../types';
type AuthValue={session:Session|null;loading:boolean;error:string;login:(e:string,p:string)=>Promise<void>;logout:()=>Promise<void>;refresh:()=>Promise<Session|null>;requestPasswordReset:(e:string)=>Promise<void>;updatePassword:(p:string)=>Promise<void>;hasPermission:(p:string)=>boolean};const AuthContext=createContext<AuthValue|null>(null);const message=(e:unknown)=>e instanceof Error?e.message:'No fue posible completar la operación.';
export function AuthProvider({children}:{children:ReactNode}){const[session,setSession]=useState<Session|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{let active=true;authService.current().then(value=>{if(active)setSession(value)}).catch(e=>{if(active)setError(message(e))}).finally(()=>{if(active)setLoading(false)});const subscription=authService.listen((event,authSession)=>{if(!active)return;if(event==='SIGNED_OUT'){setSession(null);setLoading(false);return}if(authSession&&(event==='SIGNED_IN'||event==='TOKEN_REFRESHED'||event==='USER_UPDATED')){setLoading(true);setTimeout(()=>hydrateSession(authSession).then(value=>{if(active){setSession(value);setError('')}}).catch(e=>{if(active){setSession(null);setError(message(e))}}).finally(()=>{if(active)setLoading(false)}),0)}});return()=>{active=false;subscription.unsubscribe()}},[]);const refresh=useCallback(async()=>{setLoading(true);setError('');try{const value=await authService.current();setSession(value);return value}finally{setLoading(false)}},[]);const login=async(email:string,password:string)=>{setLoading(true);setError('');try{setSession(await authService.login(email,password))}finally{setLoading(false)}};const logout=async()=>{await authService.logout();setSession(null)};return <AuthContext.Provider value={{session,loading,error,login,logout,refresh,requestPasswordReset:authService.requestPasswordReset,updatePassword:authService.updatePassword,hasPermission:permission=>session?.permissions.includes(permission)===true}}>{children}</AuthContext.Provider>}
export function useAuth(){const value=useContext(AuthContext);if(!value)throw new Error('AuthProvider requerido');return value}
