import{useState}from'react';
import{NavLink,Outlet,useNavigate}from'react-router-dom';
import{Bell,LogOut,Menu,X}from'lucide-react';
import{navigationItems,navigationSections}from'../app/navigation';
import{useAuth}from'../context/AuthContext';
import{createPermissionReader,visibleNavigationItems}from'../infrastructure/permissions/permissionAdapter';
export function AdminLayout(){const[open,setOpen]=useState(false);const{session,logout}=useAuth();const nav=useNavigate();const items=visibleNavigationItems(navigationItems,createPermissionReader());return <div className="shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand"><span className="brand-mark">O</span><div><b>OSINET</b><small>TIME ERP ENTERPRISE</small></div><button className="icon mobile" onClick={()=>setOpen(false)} aria-label="Cerrar menú"><X/></button></div><nav>{navigationSections.map(section=>{const children=items.filter(item=>item.section===section);return children.length?<div className="nav-section" key={section}><span>{section}</span>{children.map(({to,label,icon:Icon})=><NavLink key={to} to={to} onClick={()=>setOpen(false)}><Icon size={19}/>{label}</NavLink>)}</div>:null})}</nav><button className="logout" onClick={()=>{logout();nav('/login')}}><LogOut size={18}/>Cerrar sesión</button></aside><div className="workspace"><header className="topbar"><button className="icon mobile" onClick={()=>setOpen(true)} aria-label="Abrir menú"><Menu/></button><div className="company"><span className="live-dot"/>ACME Dominicana · Oficina Central</div><div className="user"><button className="icon" aria-label="Notificaciones" onClick={()=>nav('/dashboard')}><Bell/><i>3</i></button><span className="avatar">MG</span><div><b>{session?.name}</b><small>{session?.role}</small></div></div></header><main><Outlet/></main></div></div>}
